#!/usr/bin/env ruby
# frozen_string_literal: true

require './lib/hex_to_filter'

solver = HexToFilter.new(format: 'css')

puts solver.get_filters('#ffffff')
puts solver.get_filters('#4a5455')
puts solver.get_filters('#000000')
puts solver.get_filters('#ff0000')
